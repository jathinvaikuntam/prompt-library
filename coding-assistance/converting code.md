# converting code

## Purpose

To convert code between languages using provided patterns.

# Prompt 

Convert the following Python functions to JavaScript.

# Example:

def add(a, b): return a + b -> const add = (a, b) => a + b;

# Example Input:

 def greet(name): print(f"Hello, {name}")

# Example Output:

const greet = (name) => console.log(\Hello, ${name}`);`